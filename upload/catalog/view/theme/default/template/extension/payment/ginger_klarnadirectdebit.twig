<form action="{{ action }}" method="post" id="klarnadirectdebit-form-payment">
        <div class="form-group pull-right">
        <div class="right col-sm-3">
            <input type="submit" value="{{ button_confirm }}" id="klarnadirectdebit-button-payment" class="button btn btn-primary"/>
        </div>
    </div>
</form>